{
	"scripts": {
		"build": "npm run clean && webpack",
		"build:watch": "npm run clean && webpack --watch",
		"clean": "rimraf addon/js",
		"start:firefox": "web-ext run --source-dir ./addon/"
	},
	"devDependencies": {
		"@types/firefox": "^0.0.34",
		"@types/firefox-webext-browser": "^120.0.0",
		"rimraf": "^5.0.5",
		"web-ext": "^7.9.0",
		"webpack": "^5.89.0",
		"webpack-cli": "^5.1.4"
	},
	"dependencies": {
		"color-parse": "^2.0.0",
		"tinycolor2": "^1.6.0"
	}
}
